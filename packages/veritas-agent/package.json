{
  "name": "veritas-agent",
  "version": "0.1.0",
  "description": "VERITAS agent SDK — key generation, registration, credential minting, and PoP signing for the VERITAS identity protocol",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kodydpa-hub/veritas.git",
    "directory": "packages/veritas-agent"
  },
  "keywords": ["veritas", "agent-identity", "verifiable-credential", "icp", "did", "agent-sdk"],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "veritas-verify": "^0.1.0",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
